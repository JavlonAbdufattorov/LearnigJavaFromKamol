#  Примитивные типы данных и единицы информации в Java

---

## 1️ Примитивные типы данных

В Java есть **8 примитивных типов**:

| Тип       | Размер    | Значение / Пример              | Описание                                 |
|-----------|----------|-------------------------------|-----------------------------------------|
| `byte`    | 1 байт   | -128 до 127                   | Малые целые числа, экономит память      |
| `short`   | 2 байта  | -32,768 до 32,767             | Целые числа средних размеров            |
| `int`     | 4 байта  | -2,147,483,648 до 2,147,483,647 | Наиболее часто используемый целый тип   |
| `long`    | 8 байт   | ±9.22 × 10¹⁸                 | Большие целые числа                      |
| `float`   | 4 байта  | 3.14, 0.5, -2.0               | Числа с плавающей точкой (небольшая точность) |
| `double`  | 8 байт   | 3.1415926535                  | Числа с плавающей точкой высокой точности |
| `char`    | 2 байта  | `'A'`, `'я'`, `'\u0041'`      | Один символ (Unicode)                   |
| `boolean` | 1 бит    | `true` / `false`             | Логическое значение                     |

---

## 2️ Разница между битом и байтом

### 2.1 Бит (bit)
- **Бит** — минимальная единица информации в компьютере.  
- Может принимать только два значения: `0` или `1`.  
- Используется для хранения самой простой информации: включено/выключено, истина/ложь, черный/белый пиксель.  

**Пример:**


### 2.2 Байт (byte)
- **Байт** — это набор из **8 бит**.  
- Основная единица хранения информации в памяти.  
- Один байт может хранить **256 различных значений**:


- Часто один байт используется для хранения **одного символа**, например `'A'` или `'я'`.  

**Пример:**


### 2.3 Связь между битами и байтами
- 1 байт = 8 бит  
- 1 килобайт (KB) ≈ 1024 байт  
- 1 мегабайт (MB) ≈ 1024 KB  
- 1 гигабайт (GB) ≈ 1024 MB  

**Формулы:**


**Пример для наглядности:**
- Число 255 в бинарном виде `11111111` = 8 бит = 1 байт
- 16 бит = 2 байта
- 32 бит = 4 байта и т.д.

---

## 3️
Мини-таблица единиц измерения информации

| Единица       | Размер в битах | Пример                 |
|---------------|----------------|-----------------------|
| 1 бит (bit)   | 1              | 0 или 1               |
| 1 байт (B)    | 8              | 1 символ (ASCII)      |
| 1 килобайт (KB)| 1024 B         | Страница текста       |
| 1 мегабайт (MB)| 1024 KB        | Фото, небольшой документ |
| 1 гигабайт (GB)| 1024 MB        | Фильм, игра           |
| 1 терабайт (TB)| 1024 GB        | Жёсткий диск          |
# Signed и Unsigned числа в двоичном коде

---

## 1️ Unsigned (без знака)

**Unsigned** — это число **без знака**, то есть:
- может быть **только неотрицательным (≥ 0)**
- **не поддерживает отрицательные значения**

Все биты используются **только для значения**.

###  Пример: 8-битное unsigned число
00000000 = 0

00000001 = 1

00000010 = 2

...

11111111 = 255

Максимальное значение:
2^8 - 1 = 255


---

## 2️ Signed (со знаком)

**Signed** — это число **со знаком**, то есть:
- может быть **положительным или отрицательным**
- **старший (левый) бит** — это **знаковый бит**

| Старший бит | Значение |
|------------|----------|
| `0` | Число положительное |
| `1` | Число отрицательное |

В современных компьютерах используется **дополнительный код (Two’s Complement)**.

---

## 3️ Диапазон значений Signed чисел

Для `n` бит:
-2^(n-1) ≤ x ≤ 2^(n-1) - 1


###  Пример: 8-битное signed число

| Двоичный код | Десятичное значение |
|--------------|--------------------|
| `00000000` | 0 |
| `00000001` | 1 |
| `01111111` | 127 |
| `10000000` | -128 |
| `11111111` | -1 |

---

## 4️ Как получить отрицательное число (Two’s Complement)

Чтобы получить **-X** из **X**:

###  Алгоритм:
1. Взять двоичное представление числа  
2. Инвертировать все биты (`0 → 1`, `1 → 0`)  
3. Прибавить `1`

### 📌 Пример: `+5 → -5` (8 бит)



+5 = 00000101

~ = 11111010

+1 = 11111011 → -5

